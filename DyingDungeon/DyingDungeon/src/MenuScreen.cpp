#include "MenuScreen.h"

MenuScreen::MenuScreen(std::string _menuName)
{
	// Set the name of this menu to the one that was passed in through the constuctor
	SetMenuName(_menuName);
}

MenuScreen::~MenuScreen()
{

}